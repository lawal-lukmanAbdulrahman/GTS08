import NotFound from "./(storefront)/not-found";
import StorefrontLayout from "./(storefront)/layout";

export default function RootNotFound() {
  return (
    <StorefrontLayout>
      <NotFound />
    </StorefrontLayout>
  );
}
